module SomeMod exposing (..)

import MyMod exposing
    ( map, foldl
    , Maybe, Possibly
    , Result(..)
    , MyList(Empty), Tree(Node, Value, Special) )

import otherMod exposing (Cons)

func x =
    42 + 24
